$(document).ready(function() {
    /*----------------- URL для AJAX запросов ----------------*/
    let ajaxRoot = $('[name="ajaxroot"]').attr('content');
    /*----------------- URL для AJAX запросов ----------------*/
});